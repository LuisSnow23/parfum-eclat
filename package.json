{
  "name": "parfum-eclat",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix backend && cd backend && npm rebuild better-sqlite3 && cd .. && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": "22.x"
  }
}